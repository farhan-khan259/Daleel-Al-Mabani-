Drop the services hero image here as `services-hero.jpg`.

You provided an image via the editor; to use it locally, save that image to this path:

assets/services-hero.jpg

If you want me to embed a remote URL instead, paste the URL and I'll update `services.html` to use it directly.
